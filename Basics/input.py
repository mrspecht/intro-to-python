# Input

name = input("What's your name? ")                                              # The input() function pauses the program and waits for
age = input("How old are you? ")                                                # the user to enter some text. input() takes one argument
                                                                                # and returns a string
print(name + ", " + age + " years old")
